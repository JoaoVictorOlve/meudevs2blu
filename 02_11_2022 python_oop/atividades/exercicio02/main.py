from conta import Conta

#variável de referência acaba se tornando objeto
#objeto <- conta

conta = Conta("João", 123, 200, 500) #parenteses está referenciando construtor lá dentro
print(conta)

#Código procedural <- começa pelo menu, recebe dados, controller manipula e devolve
#POO <- Temos algo dentro da classe que tem características e atributos (reverso do que é o código procedeural)